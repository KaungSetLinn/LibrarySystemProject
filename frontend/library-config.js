/*
 * -----------------------------------------------------------------------------
 * ファイル名   : library-config.js
 * 概要         : 図書予約システムのブラウザ実行時の既定設定を保持する。
 *                library-system-config.txt の参照結果にかかわらず、
 *                起動時に必要な既定値を提供する。
 * 作成者       : Y.Toyoda
 * 作成日       : 2026-04-17
 * -----------------------------------------------------------------------------
 */

// ブラウザ起動時に参照する既定設定を定義する。
window.LIBRARY_CONFIG_FALLBACK = {
  // データベース種別は初期要件に合わせて Excel を既定値とする。
  dbType: "SQLite",
  // 利用者が予約できる最大冊数を 3 冊に設定する。
  maxReservations: 3,
  // 検索結果は 1 ページ 10 件表示とする。
  searchPageSize: 10,
  // 設定テキストファイル名を保持する。
  configFileName: "library-system-config.txt"
};
