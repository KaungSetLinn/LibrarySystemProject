/*
 * -----------------------------------------------------------------------------
 * ファイル名   : library-config.js
 * 概要         : 図書予約システムのブラウザ実行時の既定設定を保持する。
 *                library-system-config.txt の参照結果にかかわらず、
 *                起動時に必要な既定値を提供するフォールバック層。
 * 作成者       : Y.Toyoda
 * 作成日       : 2026-04-17
 * 改訂         : 2026-05-12 シークレット機能(豊田v3.0.6拡張)の既定値追加
 * -----------------------------------------------------------------------------
 */

// ブラウザ起動時に参照する既定設定を定義する。
window.LIBRARY_CONFIG_FALLBACK = {
  // -------------------------------------------------------------------------
  // 基本設定(カゥン氏 backend と直結する項目)
  // -------------------------------------------------------------------------
  // データベース種別。SQLite を既定値とする(構成案B確定値)。
  dbType: "SQLite",
  // 利用者が予約できる最大冊数を 3 冊に設定する。
  maxReservations: 3,
  // 検索結果は 1 ページ 10 件表示とする。
  searchPageSize: 10,
  // 設定テキストファイル名を保持する。
  configFileName: "library-system-config.txt",

  // -------------------------------------------------------------------------
  // シークレット機能の既定値(豊田 v3.0.6 拡張モード)
  // -------------------------------------------------------------------------
  // secretMode は config ファイルで明示的に ON にされない限り無効。
  secretMode: false,
  // 機能トグルの既定値(secretMode=ON 時のみ評価される)
  features: {
    favorites:       true,   // お気に入り CRUD(localStorage)
    notifications:   true,   // 通知一覧と既読化
    history:         true,   // 予約履歴の閲覧
    auditLog:        true,   // 監査ログの記録
    duplicateGuard:  true,   // 同一書籍の重複登録ガード(W19)
    dashboardKpi:    true,   // 拡張ダッシュボード KPI 表示
    localExport:     true    // ローカルデータ JSON 出力
  },
  // 画面右下のシークレットモードインジケーター
  secretIndicator: true
};
